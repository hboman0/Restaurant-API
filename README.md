# Restaurant Management System API

## Description
Backend API for managing restaurant menu items and reservations. Built with Node.js and Express using JSON storage.

## Installation
1. Clone repository: git clone <repo-link>
2. `npm install`
3. `node server.js`
Server runs on http://localhost:3000

## Routes

### Demo
- `GET /` → Server running
- `GET /hello` → Hello JSON
- `GET /time` → Current server time
- `GET /status` → Status OK

### Menu Items
- `GET /menu-items` → Get all menu items
- `POST /menu-items` → Create a menu item
- `PUT /menu-items/:id` → Update a menu item
- `DELETE /menu-items/:id` → Delete a menu item

### Reservations
- `GET /reservations` → Get all reservations
- `POST /reservations` → Create a reservation
- `PUT /reservations/:id` → Update a reservation
- `DELETE /reservations/:id` → Delete a reservation

### Examples
<img width="462" height="612" alt="Снимок экрана 2025-12-19 014710" src="https://github.com/user-attachments/assets/07b1fec2-0a75-4ce9-bca4-d17592b674ee" />
<img width="492" height="588" alt="Снимок экрана 2025-12-19 014926" src="https://github.com/user-attachments/assets/6e3792da-32ce-44b9-b7cf-216263659a21" />
<img width="455" height="533" alt="Снимок экрана 2025-12-19 015140" src="https://github.com/user-attachments/assets/b672b361-9cf9-4aca-8b1e-9714b04f6da1" />


